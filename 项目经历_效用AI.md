# 项目经历｜基于效用 AI 的虚拟角色自主行为决策系统

**个人项目 · C++17 · 2026.08 – 至今**

## 项目概述
从零设计并实现一套面向桌面虚拟角色的需求驱动（Drive-based）**效用 AI 元控制器**，使角色依据昼夜、天气与五项身心属性（心情 / 社交 / 活力 / 清醒 / 饱腹）自主决策行为，替代硬编码脚本。

## 核心工作
- 设计**响应曲线（Response Curve）**软阈值：以偏差平方映射 0~1 的"急迫度"，消除硬阈值带来的决策抖动，让需求强度连续影响选择。
- 提出**效用评分公式** `score = Σ w · drive · improve`，其中 `improve = |ideal − cur| − |ideal − new|` 度量动作朝理想值靠拢程度，天然抑制过饱和（已满足的需求不再重复触发）。
- 实现 **tick 驱动模拟主循环**与 **commitment 机制**（动作持续 N 个 tick 后才重新决策），解决动作高频抖动（thrashing）。
- 建模**世界动态**（昼夜随真实时间推进、天气周期切换）与**角色动态**（饱腹/社交随时间衰减，清醒昼夜反向、活力雨天下降、心情受昼夜与天气影响），并用声明式 `ActionEffect` 描述各动作对属性的增减。

## 成果
系统在数十 tick 内自主完成"互动补社交 → 清醒不足转睡觉 → 恢复后再互动"等行为序列，验证了纯效用 AI 决策的有效性；架构预留接口，可无缝接入强化学习执行器与 LLM 反思层。

## 技术栈
C++17 · 面向对象设计 · 贪心效用最大化（argmax）· 单仓可编译运行
